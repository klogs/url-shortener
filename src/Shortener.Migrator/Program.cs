// Shortener.Migrator
// Runs database migrations. Never executed automatically at application startup.
// Usage: dotnet run --project src/Shortener.Migrator

Console.WriteLine("Shortener Migrator — no migrations registered yet.");
